$(".liked-btn").on('ajax:success', function(){
  $(this).parent().parent().find('.like_count')
  .html('<%= @post.get_likes.size %>');
  $(this).closest('.liked-btn').html('💙');
});

$('.liked-btn').on('ajax:success', function(){
  $(this).parent().parent().find('.like_count')
  .html('<%= @post.get_likes.size %>');
  $(this).closest('.like-btn').html('🤍');
});